---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
};
const { class: className = '' } = Astro.props;
---

<div class="w-full overflow-auto">
  <table class:list={twMerge(['w-full text-sm', className])}>
    <slot />
  </table>
</div>
