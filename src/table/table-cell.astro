---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
};
const { class: className = '' } = Astro.props;
---

<td class:list={twMerge(['p-2 align-middle', className])}>
  <slot />
</td>
