---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
};
const { class: className = '' } = Astro.props;
---

<tbody class:list={twMerge(['[&_tr:last-child]:border-0', className])}>
  <slot />
</tbody>
