---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
  'class:list'?: string;
};
const { class: className = '', 'class:list': classList = '' } = Astro.props;
---

<tbody class:list={twMerge(['[&_tr:last-child]:border-0', className, classList])}>
  <slot />
</tbody>
