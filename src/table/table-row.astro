---
import { twMerge } from 'tailwind-merge';

type Props = JSX.IntrinsicElements['tr'];
const { class: className = '' } = Astro.props;
---

<tr
  class={twMerge('border-b transition-colors hover:bg-muted/50 data-[state=selected]:bg-muted', className)}
  {...Astro.props}
>
  <slot />
</tr>
