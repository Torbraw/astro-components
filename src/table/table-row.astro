---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
};
const { class: className = '' } = Astro.props;
---

<tr class:list={twMerge(['border-b transition-colors hover:bg-muted/50 data-[state=selected]:bg-muted', className])}>
  <slot />
</tr>
