---
import { twMerge } from 'tailwind-merge';

type Props = JSX.IntrinsicElements['thead'];
const { class: className = '' } = Astro.props;
---

<thead class={twMerge('[&_tr]:border-b', className)} {...Astro.props}>
  <slot />
</thead>
