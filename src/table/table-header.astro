---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
  'class:list'?: string;
};
const { class: className = '', 'class:list': classList = '' } = Astro.props;
---

<thead class:list={twMerge(['[&_tr]:border-b', className, classList])}>
  <slot />
</thead>
