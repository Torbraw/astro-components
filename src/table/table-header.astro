---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
};
const { class: className = '' } = Astro.props;
---

<thead class:list={twMerge(['[&_tr]:border-b', className])}>
  <slot />
</thead>
