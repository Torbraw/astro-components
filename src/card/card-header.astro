---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
  'class:list'?: string;
};
const { class: className = '', 'class:list': classList = '' } = Astro.props;
---

<div class:list={twMerge(['flex flex-col space-y-1.5 p-6', className, classList])}>
  <slot />
</div>
