---
import { twMerge } from 'tailwind-merge';

type Props = JSX.IntrinsicElements['h3'];
const { class: className = '' } = Astro.props;
---

<h3 class={twMerge('font-semibold leading-none tracking-tight', className)} {...Astro.props}>
  <slot />
</h3>
