---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
  'class:list'?: string;
};
const { class: className = '', 'class:list': classList = '' } = Astro.props;
---

<div class:list={twMerge(['p-6 pt-0', className, classList])}>
  <slot />
</div>
