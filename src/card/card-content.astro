---
import { twMerge } from 'tailwind-merge';

type Props = JSX.IntrinsicElements['div'];
const { class: className = '' } = Astro.props;
---

<div class={twMerge('p-6 pt-0', className)} {...Astro.props}>
  <slot />
</div>
