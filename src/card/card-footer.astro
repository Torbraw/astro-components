---
import { twMerge } from 'tailwind-merge';
type Props = {
  class?: string;
  'class:list'?: string | string[];
};
const { class: className = '', 'class:list': classList = '' } = Astro.props;
---

<div class={twMerge('justify-between p-6 pt-0 items-center flex', className, classList)}>
  <slot />
</div>
