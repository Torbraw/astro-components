---
import { twMerge } from 'tailwind-merge';
export type Props = {
  class?: string;
};
const { class: className = '' } = Astro.props;
---

<p class:list={twMerge(['text-sm text-muted-foreground', className])}>
  <slot />
</p>
