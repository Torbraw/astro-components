---
import { twMerge } from 'tailwind-merge';

type Props = JSX.IntrinsicElements['div'];
const { class: className = '' } = Astro.props;
---

<div class={twMerge('flex flex-col-reverse sm:flex-row sm:justify-end sm:space-x-2', className)} {...Astro.props}>
  <slot />
</div>
